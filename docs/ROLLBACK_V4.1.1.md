# FLAP Fly Agent 4.1.1 回滚指引

1. 完全退出 App 并备份 `%LOCALAPPDATA%/FLAP Fly Agent/`。
2. 卸载 4.1.1；安装器不会主动删除用户数据。
3. 安装 4.0.0，或等待更新版本。不要回到已被取代的 4.1.0。
4. 独立 HTML 没有安装状态，停止使用并删除下载文件即可；它没有读取或迁移 App 数据。

已经部署的合约和成功发布的测试卡带不可回滚或删除。软件或 HTML 文件回滚不会改变
链上状态；若合约存在问题，应停止传播旧地址并部署经过修复和重新确认的新版本。
