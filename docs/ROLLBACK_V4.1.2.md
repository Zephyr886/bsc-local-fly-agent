# FLAP Fly Agent 4.1.2 回滚指引

1. 完全退出 App 并备份 `%LOCALAPPDATA%/FLAP Fly Agent/`。
2. 卸载 4.1.2；安装器不会主动删除用户数据。
3. 如必须回滚，可重新安装 4.1.1；但 4.1.1 安装版仍存在 V4 导入资源缺失问题。
4. 已部署的 Registry V4 合约和链上卡带不受 App 升级或回滚影响。

优先建议保留 4.1.2。回滚只用于排查其他兼容问题，不是 V4 导入故障的解决方案。
